import Vue from 'vue';
import {
	Button,
	Dialog,
	Form,
	FormItem,
	Input,
	Select,
	Menu,
	MenuItem,
	Row,
	Col,
	Dropdown,
	DropdownItem,
	DropdownMenu,
	Loading,
	Timeline,
	TimelineItem,
	Card,
	Tag
} from 'element-ui';

Vue.use(Button)
	.use(Dialog)
	.use(Form)
	.use(FormItem)
	.use(Input)
	.use(Select)
	.use(Menu)
	.use(MenuItem)
	.use(Row)
	.use(Col)
	.use(Dropdown)
	.use(DropdownItem)
	.use(DropdownMenu)
	.use(Loading)
	.use(Timeline)
	.use(TimelineItem)
	.use(Card)
	.use(Tag);

import CollapseTransition from 'element-ui/lib/transitions/collapse-transition';
